~/.bashrc

#change default terminal editor from vim to nano
export EDITOR='nano'
export VISUAL='nano'

#alias variables shortcut
alias update='sudo apt-get update'
alias upgrade='sudo apt-get upgrade'
alias remove='sudo apt-get auto-remove'
alias vultr='ssh root@45.77.118.131'
alias gitconfig='git config --user.name "kmykoh97"'


#functions shortcut
updateupgrade() {
        #builtin sudo apt-get update "$*" && sudo apt-get upgrade#cannot run
}
alias kmykohuu='updateupgrade'

cdls() {
        builtin cd "$*" && ls
}
alias cd='cdls'



source .bashrc
